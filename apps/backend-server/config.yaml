server:
  port: 3001
  mode: debug

database:
  host: localhost
  port: 3306
  user: root
  password: root
  dbname: crmb
  charset: utf8mb4

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "crmb-jwt-secret-key-2026"
  expire_hours: 24

minio:
  endpoint: localhost:9000
  access_key_id: minioadmin
  secret_access_key: minioadmin
  bucket_name: crmb
  use_ssl: false

log:
  level: info
  file: logs/crmb.log